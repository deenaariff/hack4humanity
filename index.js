var app = require('express');
var PORT = 3000;

// Modules 
var users = 
var events = 
var requesterAPI = require('');
var workerAPI = require('');
var dashboard API = require('');

app.listen(PORT);
